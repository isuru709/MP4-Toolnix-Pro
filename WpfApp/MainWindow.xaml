<Window x:Class="MP4_Toolnix.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MP4 Toolnix (Pro)" Height="560" Width="960"
        Background="#FF1E1E1E"
        WindowStartupLocation="CenterScreen" Icon="/film.png">

    <Window.Resources>
        <!-- Modern Color Palette -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF0078D4"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#FF106EBE"/>
        <SolidColorBrush x:Key="CardBrush" Color="#FF2D2D30"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FFB0B0B0"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#FF404040"/>
        <SolidColorBrush x:Key="HoverBrush" Color="#FF3F3F46"/>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FFE74C3C"/>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ListBox Style -->
        <Style x:Key="ModernListBox" TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer Padding="4">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ListView Style -->
        <Style x:Key="ModernListView" TargetType="ListView">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListView">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer>
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern GroupBox Style -->
        <Style x:Key="ModernGroupBox" TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" 
                                    Background="{StaticResource HoverBrush}" 
                                    CornerRadius="6,6,0,0" 
                                    Padding="12,8">
                                <ContentPresenter ContentSource="Header" 
                                                TextBlock.FontWeight="SemiBold" 
                                                TextBlock.FontSize="14"/>
                            </Border>

                            <Border Grid.Row="1" 
                                    Background="{StaticResource CardBrush}" 
                                    BorderBrush="{StaticResource BorderBrush}" 
                                    BorderThickness="1" 
                                    CornerRadius="0,0,6,6" 
                                    Padding="8">
                                <ContentPresenter/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ProgressBar Style -->
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="9">
                            <Grid>
                                <Border Name="PART_Track" 
                                        Background="{TemplateBinding Background}" 
                                        CornerRadius="9"/>
                                <Border Name="PART_Indicator" 
                                        Background="{TemplateBinding Foreground}" 
                                        HorizontalAlignment="Left" 
                                        CornerRadius="9"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBlock Style -->
        <Style x:Key="ModernTextBlock" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- Header TextBlock Style -->
        <Style x:Key="HeaderTextBlock" TargetType="TextBlock" BasedOn="{StaticResource ModernTextBlock}">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Inputs management -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical" Grid.Column="0" Margin="0,0,8,0">
                <TextBlock Text="Inputs (video/audio/subtitle):" 
                          Margin="0,0,0,4" 
                          Style="{StaticResource HeaderTextBlock}"/>
                <ListBox x:Name="LstInputs" 
                        Height="120" 
                        SelectionMode="Extended" 
                        Style="{StaticResource ModernListBox}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Vertical">
                <Button Content="Add Files..." 
                       Width="140" 
                       Margin="0,0,0,6" 
                       Click="AddInputs_Click" 
                       Style="{StaticResource ModernButton}"/>
                <Button Content="Remove Selected" 
                       Width="140" 
                       Margin="0,0,0,6" 
                       Click="RemoveInputs_Click" 
                       Style="{StaticResource DangerButton}"/>
                <Button Content="DeMUX" 
                       Width="140" 
                       Click="Probe_Click" 
                       Style="{StaticResource SecondaryButton}"/>
            </StackPanel>
        </Grid>

        <!-- Output -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,8">
            <TextBlock Text="Output:" 
                      VerticalAlignment="Center" 
                      Margin="0,0,8,0" 
                      Style="{StaticResource HeaderTextBlock}"/>
            <TextBox x:Name="TxtOutput" 
                    Width="640" 
                    Margin="0,0,8,0" 
                    Style="{StaticResource ModernTextBox}"/>
            <Button Content="Browse..." 
                   Width="100" 
                   Click="BrowseOutput_Click" 
                   Style="{StaticResource SecondaryButton}"/>
        </StackPanel>

        <!-- Streams -->
        <GroupBox Grid.Row="2" 
                 Header="Streams (select which to keep)" 
                 Style="{StaticResource ModernGroupBox}">
            <DockPanel>
                <ListView x:Name="ListStreams" 
                         SelectionMode="Extended" 
                         Style="{StaticResource ModernListView}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Keep" Width="60">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding Keep, Mode=TwoWay}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Input" DisplayMemberBinding="{Binding Input}" Width="60"/>
                            <GridViewColumn Header="Index" DisplayMemberBinding="{Binding Index}" Width="60"/>
                            <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" Width="80"/>
                            <GridViewColumn Header="Codec" DisplayMemberBinding="{Binding Codec}" Width="120"/>
                            <GridViewColumn Header="Language" DisplayMemberBinding="{Binding Language}" Width="100"/>
                            <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}" Width="180"/>
                            <GridViewColumn Header="Duration" DisplayMemberBinding="{Binding Duration}" Width="100"/>
                            <GridViewColumn Header="File" DisplayMemberBinding="{Binding FileName}" Width="240"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </DockPanel>
        </GroupBox>

        <!-- Actions/status -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,8,0,8">
            <Button x:Name="BtnRemux" 
                   Content="Remux (Copy Streams)" 
                   Width="200" 
                   Click="Remux_Click" 
                   Style="{StaticResource ModernButton}"/>
            <TextBlock x:Name="LblStatus" 
                      Margin="12,0,0,0" 
                      VerticalAlignment="Center" 
                      Style="{StaticResource ModernTextBlock}"/>
        </StackPanel>

        <StackPanel Grid.Row="4">
            <ProgressBar x:Name="Progress" 
                        Height="18" 
                        Minimum="0" 
                        Maximum="100" 
                        Style="{StaticResource ModernProgressBar}"/>
        </StackPanel>
    </Grid>
</Window>